# 本次开发问题与经验

## Git 状态

- 当前分支：`main`
- 最新提交：`cef5476 fix: make auto-created model pools callable`
- 主要功能提交：
  - `7faf3a9 feat: auto-create pools for platform models`
  - `cef5476 fix: make auto-created model pools callable`
- 远端：`origin -> git@github.com:0311yet/ai_api.git`
- 当前工作区：干净，没有未提交修改、未跟踪文件或差异。

## 本次实现的功能

平台配置中的每个模型会自动对应一个同名模型池，并创建平台到该模型池的路由成员。

当两个不同平台配置了相同模型时：

- 只创建一个同名模型池；
- 两个平台都成为该模型池的路由成员；
- 由模型池的 `priority`、`round_robin`、`weighted` 或 `random` 策略选择上游；
- 平台的多个 API Key 也可以参与负载均衡和故障切换。

编辑平台模型列表时采用“只新增、不自动删除”的策略，避免误删已有池或影响线上请求。

## 遇到的问题

### 1. 平台模型字段不等于可调用模型

最初平台的 `models` 字段只是配置和展示信息，代理实际只查询 `pool_items`。因此平台虽然显示了模型，但 `/v1/models` 仍然为空，请求也会提示没有找到可用模型。

经验：配置数据、路由数据和对外 API 数据必须形成完整的数据流，不能只完成管理页面或数据库字段。

### 2. 自动创建模型池后，旧客户端 Key 仍可能无法调用

客户端 Key 绑定的是一个具体模型池。如果代理只使用 Key 的绑定池，那么新增的同名模型池不会被发现。

后续修复为：

- 绑定池包含请求模型时，优先使用绑定池；
- 绑定池不包含请求模型时，查找请求模型同名的活动模型池；
- Key 未绑定池时，也可以使用活动模型池；
- `allowed_models` 仍然作为客户端权限限制。

### 3. 历史平台不会自动补齐模型池

自动同步只覆盖新建或编辑平台，历史数据仍可能缺少模型池。因此增加了应用启动时的补齐逻辑，对所有已有平台模型执行同步。

经验：新增自动化数据逻辑时，要考虑历史数据迁移和重复执行。同步函数应当幂等，重复执行不能产生重复池或重复路由成员。

### 4. 部署目录与 Git 工作树不一致

线上曾经存在多个目录：旧部署目录、发布目录以及父目录 Git 工作树。服务器执行 `git fetch` 时更新的是父目录，但 Docker 构建使用的是另一个未更新的发布目录，导致服务器显示已拉取新提交，容器实际仍运行旧代码。

经验：部署脚本必须明确：

- Git 仓库根目录；
- Docker build context；
- 数据库目录；
- 当前运行容器使用的镜像和源码版本。

部署后不能只检查容器健康状态，还要检查容器内的代码版本或关键代码特征。

### 5. Docker 缓存掩盖了代码是否更新

服务器首次重建时大量使用旧缓存层，健康检查虽然通过，但无法证明业务代码已经更新。最终使用了正确发布目录和 `--no-cache` 构建，并在镜像内检查自动建池代码。

经验：涉及关键业务修复时，至少执行一次无缓存构建，或通过镜像标签、提交哈希和容器内文件校验确认版本。

### 6. 旧版 docker-compose 重建兼容性问题

服务器使用 `docker-compose 1.29`，在新 Docker 镜像元数据下重建时出现 `ContainerConfig` 错误。

处理方式是保留数据挂载目录，单独删除旧容器，并使用明确的镜像 ID 重新运行容器。以后应升级 Compose，或使用更明确、可重复的发布脚本。

## 数据与安全注意事项

- 当前数据库使用：`data/ai_gateway.db`。
- 服务器上曾存在一个空的 `app.db`，不能仅凭文件名擅自迁移或重命名。
- 平台 API Key 已在管理响应中脱敏，但数据库仍是明文存储，生产环境应考虑加密存储和密钥轮换。
- 管理员密码不应继续使用默认值，应通过服务器环境变量或密钥管理系统配置强密码。
- 部署不应把 root 密码写入脚本、Git 或日志，推荐使用 SSH 部署密钥。
- GitHub 仓库应确认可见性、访问权限和服务器拉取权限，服务器使用只读 Deploy Key 更合适。

## 建议的后续改进

1. 增加 pytest 测试，覆盖新增平台、编辑新增模型、重复同步、同名模型合并和客户端 Key 路由。
2. 增加启动同步的统计日志，例如新增了多少模型池和路由成员。
3. 为模型池和路由成员增加唯一约束，进一步保证并发写入时不重复。
4. 使用 Alembic 管理正式数据库迁移，减少手写兼容迁移的风险。
5. 使用固定版本的 Docker Compose 或 Docker Compose v2，并将构建提交哈希写入镜像标签。
6. 将部署目录、数据目录、镜像标签和健康检查整合成一个可重复执行的部署脚本。
7. 多进程部署时，将轮询位置等路由状态放到共享存储，避免每个进程维护独立状态。

## 验证清单

- `python -m compileall -q backend/app`
- `git diff --check`
- 前端 `pnpm build`
- `/healthz` 返回 HTTP 200
- `/v1/models` 返回自动创建的模型池
- 管理端模型池页面能看到平台模型
- 两个平台配置同名模型后，同名池包含两个路由成员
- 删除或禁用一个平台后，剩余平台仍能参与路由
